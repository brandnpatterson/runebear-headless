import React from 'react';

const Loading = () => {
  return (
    <div
      style={{
        marginTop: '250px',
        display: 'flex',
        height: '100vh',
        justifyContent: 'center'
      }}
    >
      <h2>Loading...</h2>
    </div>
  );
};

export default Loading;
